iterator upto(from: int, to: int): int {
  while from <= to {
    yield from;
    from = from + 1;
  }
}

let sum = 0;
for i in 1.upto(9) {
  for j in 1.upto(9) {
    sum = sum + i * j;
  }
}

assert sum == 2025;

object Rect {
  width: int,
  height: int
}

def area(r: Rect): int {
  return r.width * r.height;
}

def add(r: Rect): int {
  return r.width + r.height;
}

let recta = new Rect;
recta.width = 50;
recta.height = 40;

assert 2000 == recta.area;
assert 90 == recta.add;

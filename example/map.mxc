fn func(f: fn(int)->int, n:int) -> int {
    return f(n);
}

fn square(n: int) -> int {
    return n * n;
}

fn cube(n: int) -> int {
    return n * n * n;
}

println(func(square, 10));
println(func(cube, 10));

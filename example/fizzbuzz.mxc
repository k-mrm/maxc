fn main() -> int {
    int i = 0;
    for(i = 1; i <= 30; ++i) {
        if(i % 3 == 0 && i % 5 == 0)
            println("fizzbuzz");
        else if(i % 3 == 0)
            println("fizz");
        else if(i % 5 == 0)
            println("buzz");
        else
            println(i);
    }
}

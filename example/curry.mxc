fn add(n: int) -> fn(int)->int {
    fn add_(m: int) -> int {
        return n + m;
    }

    println("oi");
    println(add_(40));

    return add_;
}

let add10: fn(int)->int = add(10);
println(add10(50));

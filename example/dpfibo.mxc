let f = [41; 0];

def fibo(n: int): int {
    f[0] = 0;
    f[1] = 1;
    let i = 2;
    while(i <= n) {
        f[i] = f[i - 1] + f[i - 2];
        ++i;
    }

    return f[n];
}

40.fibo.println;
f.println;

iterator upto(from: int, to: int): int {
  while from <= to {
    yield from;
    from += 1;
  }
} 

iterator downto(from: int, to: int): int {
  while from >= to {
    yield from;
    from -= 1;
  }
}
